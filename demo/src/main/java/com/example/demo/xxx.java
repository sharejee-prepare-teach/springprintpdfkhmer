package com.example.demo;

public class xxx {
/*    public static void main(String[] args) {
        System.out.println("Hello World!, We are starting to use khmer battambang font.");

        Document document = new Document();
        String filePath = new ClassPathResource("kh_battambang.ttf").getPath();

        try {
            File file = new File("C:\\Users\\d.dim\\Desktop/tmp/khmer-unicode-example.pdf");

            ITextRenderer renderer = new ITextRenderer();
            PdfWriter writer = PdfWriter.getInstance(document, new FileOutputStream(file));
            document.open();
            KhmerLigaturizer d = new KhmerLigaturizer();

            String str = d.process("ផ្លែក្រូច");
            String hello = ("Hello");

            String strProcess = d.process("សូស្តី");

            BaseFont bfComic = BaseFont.createFont(filePath, BaseFont.IDENTITY_H, BaseFont.EMBEDDED);

            document.add(new Paragraph(hello + strProcess + str, new Font(bfComic, 22)));

            //document.add(new Paragraph(str, new Font(bfComic, 44)));

            ColumnText columnText = new ColumnText(writer.getDirectContent());
            columnText.setRunDirection(PdfWriter.RUN_DIRECTION_LTR);

        } catch (Exception e) {
            System.err.println(e.getMessage());
        }
        document.close();
    }*/
}
